# Unit Test Template

```ts
describe("<function>", () => {
  it("<behavior>", () => {
    // arrange

    // act

    // assert
  });
});
```

Naming rules:
- `returns ... when ...`
- `throws ... for ...`
- `does not ... at ... boundary`
