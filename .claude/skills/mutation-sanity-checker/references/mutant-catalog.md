# Mutant Catalog

1. Comparator flip: `>` <-> `>=`, `<` <-> `<=`
2. Constant tweak: `0.1` -> `0.11`
3. Boolean inversion: `if (valid)` -> `if (!valid)`
4. Arithmetic operator swap: `+` -> `-`
5. Return fallback change: `return 0` -> `return subtotal`
